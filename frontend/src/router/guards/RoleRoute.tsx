import type { User } from "@/types/user";

type RoleRouteProps = {
    allowedRoles: User['role'][]
};

const RoleRoute = () => {


    return (
        <div>

        </div>
    )
}

export default RoleRoute
