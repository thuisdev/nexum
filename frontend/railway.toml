# Railway — frontend static service
# Set root directory to `frontend` when creating the service.
# Build arg: VITE_API_URL=https://<your-backend>.up.railway.app/api

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
